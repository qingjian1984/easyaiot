# 环境标识（如实记录，宪法 §15）

- 执行日期：2026-08-07
- 执行人：青见（qingjian1984，owner 现场指令执行）
- 审批单号：无独立审批单号（owner 现场指令；正式生产窗口审批单由人工流程补登）
- 目标实例：postgres-server（Docker 容器，本地目标集成实例）
- 数据库：iot-device20
- 执行账号：postgres（实例无专用只读角色；以 PGOPTIONS 强制 default_transaction_read_only=on 保证只读，偏差如实记录）
- PostgreSQL 版本：18.4 (Debian 18.4-1.pgdg13+1)
- transaction_read_only：on（原始输出 ENV 行实测值）
- statement_timeout：300000 ms（PGOPTIONS 注入）
- 画像脚本：target-schema-profile.sql v1.2.0（sha256 前缀 736a27e0d1f2，与仓库当前 HEAD 一致）
- 结果 Schema：target-schema-profile-result.schema.json 1.1.0（sha256 前缀 0bcdd9cf7802）
- 基线：target-schema-profile-result.json（2026-08-05，sha256 前缀 3965f5a242a9）
- psql 退出码：0；原始输出以 ROLLBACK 收尾（只读事务回滚，符合预期）

## 环境性质声明（关键）

本环境唯一可达的目标实例为本地目标集成实例 iot-device20，**非独立生产实例**。
合同项 productionRerunRequired=true 指向生产环境；本次重跑能否记作「生产重跑」
并据此关闭 ADR-013/ADR-014 转 Accepted 的最后一项，由 owner 在审阅本证据包后指定
（见 verdict.md「待 owner 指定」节）。机器不代替人做该指定。
