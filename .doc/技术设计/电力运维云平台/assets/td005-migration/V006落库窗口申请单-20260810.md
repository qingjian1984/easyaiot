# V006 落库窗口申请单（TD-004 核心归属 Schema）

> 申请日期：2026-08-10
> 依据：ADR-013 1.5.4（Accepted）/ TD-004 1.0.3 / V006 专项评审报告
> 目标：`postgres-server / iot-device20` 本地目标集成实例
> 当前状态：**执行完成，验收通过**

## 1. 申请范围

仅通过 ADR-013 runner 执行 `--step V006`，新增五张空表及配套索引、复合外键和 trigger：

| 步骤 | 内容 | 类型 | SHA-256 |
|---|---|---|---|
| V006 | `power_site/power_space_node/power_circuit/power_device_asset/power_device_assignment` | 单事务 additive | `6fac9b429aae2fff34483fedc800f5d54bab8154b16953a85b2cf96f85229064` |
| U005 | 五表空表收缩候选，非普通回滚 | 单事务，未接 uninstall runner | `f3e5149c5b7219ddc46fb2d3b17fe0cc545b5c3bacc421b518aed64dc41f66b6` |

不修改既有表结构或业务数据，不导入站点/归属，不启用 `power.device.model`，不装配
`CollectorConfigReleasePort`。

## 2. 风险与锁影响

- 五张新表均为空建表；主要既有对象影响是 `power_device_asset.device_id → device.id` 外键建立时的短暂元数据锁。
- 本地目标当前 `device=4`，临时镜像执行约数秒；窗口仍设置 `lock_timeout=15s / statement_timeout=15s`，锁忙即失败关闭。
- V006 在单事务内执行，任一 DDL/COMMENT/trigger 失败全部回滚；history 仅在事务成功后登记 SUCCEEDED。
- capability 和写入口未实现，即使 Schema 成功也不会产生后台任务或业务写入。

## 3. 前置证据

1. V006 专项评审：2 HIGH 已整改关闭，Schema Candidate Accepted。
2. 第二临时评审库：首次执行、同 hash 重放、历史不可变/租户/FK/唯一反例、合法归属换版、MIG-009、U005 均 PASS。
3. 目标只读复核：`power_site` 等五表不存在，`schema_migration_history` 无 V006。
4. runner 目标 dry-run 已列出 V006；批准后执行前必须再次核对最终 hash。

## 4. 执行步骤

1. 记录批准单号，选择仓库外备份目录；
2. 只读 precheck：目标库名、PostgreSQL 版本、V001～V005 history/hash、V006 对象/history 不存在、INVALID index=0；
3. runner 自动 `pg_dump`，备份失败即停止；
4. 执行 `td005_migration.sh apply --step V006 --approval <批准单号> --yes`；
5. 执行后只读验证，不插入业务 fixture；
6. 归档 runner 输出、备份路径、history、MIG-009 和计数证据。

## 5. 执行后验收

- V006 history 为 SUCCEEDED 且 hash 精确匹配；
- 五表与三个 trigger function 存在，五表计数均为 0；
- MIG-009 PASS、invalid index=0；
- `product=4 / device=4 / product_properties=17` 与执行前一致；
- V001～V005 history/hash 不变；
- capability、事件协调器第四端口和业务初始化仍关闭。

## 6. 回滚

首选回滚是保持 capability/写入口关闭并保留 additive 空 Schema。只有 owner 另行批准 Schema 收缩、
五表逐表确认为空且备份有效时，才可独立执行 U005；任一表非空 U005 自动拒绝。禁止在故障处理中
直接 DROP、删除 history 或使用 U005 清除业务数据。

## 7. 审批

| 角色 | 意见 | 签名/日期 |
|---|---|---|
| owner | 批准在自动备份后仅执行 V006 DDL | 2026-08-10 |
| DBA/架构复核 | Schema Candidate Accepted | 2026-08-10 |

批准单号（APPROVAL）：`USER-APPROVAL-20260810-V006`

## 8. 执行结果

- 执行时间：2026-08-10 12:46（Asia/Shanghai）；runner `apply --step V006` 返回 `STEP_DONE V006` / `apply SUCCEEDED`。
- 仓库外备份：`D:\working\laoluopro\workspace\easyaiot-backups\td004-v006-window-20260810\iot-device20_20260810_124655.sql`。
- V006 history：`SUCCEEDED`；SHA-256 为 `6fac9b429aae2fff34483fedc800f5d54bab8154b16953a85b2cf96f85229064`；approval 与本单一致。
- 五张核心表均存在且计数均为 0；三个 trigger function、六个业务 trigger 均存在。
- MIG-009 PASS、invalid index=0；`product=4 / device=4 / product_properties=17` 与执行前一致；V001～V005 history/hash 未变。
- 本窗口未导入业务数据、未启用 capability、未装配 `CollectorConfigReleasePort`；详细证据见 `verification/window-execution-v006-20260810/exec-note.md`。
