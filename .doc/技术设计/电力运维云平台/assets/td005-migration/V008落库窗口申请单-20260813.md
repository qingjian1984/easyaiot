# V008 落库窗口申请单（TD-003 §10/§13 中心遥测 Inbox + TelemetryStore Schema）

> 申请日期：2026-08-13
> 依据：ADR-013 1.5.4（Accepted）/ TD-003 1.0.1 / 宪法 1.6.0 §6.2（中文注释 MUST）
> 目标：`postgres-server / iot-device20` 本地目标集成实例
> 当前状态：**本地执行完成，验收通过**（生产/共享库仍需独立审批）

## 1. 申请范围

仅通过 ADR-013 runner 执行 `--step V008`，新建 `iot_sink` schema 与两张空表及配套索引：

| 步骤 | 内容 | 类型 | SHA-256 |
|---|---|---|---|
| V008 | `iot_sink.telemetry_inbox`（中心遥测 Inbox，两层幂等）+ `iot_sink.telemetry_sample`（standard TelemetryStore 月分区时序表）+ 4 索引 | 单事务 additive | `693c0473386048567886b382c8c984ab98a267b7e7ce8659307b0d7395048469` |

V008 未配 uninstall step（U00x），属 additive 空 Schema，回滚见 §6。不修改既有表结构或业务数据。

## 2. 风险与锁影响

- 两张新表均为空建表（`CREATE TABLE IF NOT EXISTS`，幂等）；新增 `iot_sink` schema 不影响既有 schema。
- 本地目标 `iot-device20`，执行约数秒；窗口设置 `lock_timeout=15s / statement_timeout=15s`，锁忙即失败关闭。
- V008 在单事务内执行（`BEGIN → DDL/COMMENT/INDEX → COMMIT`），任一语句失败全部回滚；history 仅在事务成功后登记 SUCCEEDED。
- telemetry_inbox 上行写入与投影由 iot-sink 运行时驱动，Schema 建立本身不产生业务写入。

## 3. 前置证据

1. V008 SQL（TD-003 §10/§13）含中文 `COMMENT ON TABLE/COLUMN`（宪法 §6.2 MUST）。
2. runner dry-run 已列出 V008（`steps: ... -> V008 (txn)`），SHA-256 计算正常。
3. 目标只读复核：`iot_sink.telemetry_inbox/sample` 本次首次通过 runner 纳入 history（表对象此前由 PG 合同测试建过，`CREATE IF NOT EXISTS` 幂等跳过；history 之前无 V008 记录）。
4. 批准后执行前必须再次核对最终 hash。

## 4. 执行步骤

1. 记录批准单号，选择仓库外备份目录；
2. 只读 precheck（生产须跑；本地验证可用 `--skip-precheck`）：目标库名、PostgreSQL 版本、V001～V007 history/hash、V008 对象/history 状态、INVALID index=0；
3. runner 自动 `pg_dump`，备份失败即停止；
4. 执行 `td005_migration.sh apply --step V008 --approval <批准单号> --yes`；
5. 执行后只读验证，不插入业务 fixture；
6. 归档 runner 输出、备份路径、history 与计数证据。

## 5. 执行后验收

- V008 history 为 SUCCEEDED 且 hash 精确匹配（`693c0473...`）；
- `iot_sink.telemetry_inbox` 与 `iot_sink.telemetry_sample` 两表存在，计数均为 0；
- 索引存在：`idx_inbox_projection / idx_inbox_lease / idx_sample_query` + 两个 UNIQUE 约束索引（`uq_inbox_tenant_message / uq_sample_identity`）；
- V001～V007 history/hash 不变；
- 中文注释完整性（宪法 §6.2）：两表 `COMMENT ON TABLE` 均存在。

## 6. 回滚

V008 为 additive 空 Schema，首选回滚是保留 Schema（iot-sink 运行时未启用前无业务数据）。如需收缩：
两表确认为空且备份有效时，由 owner 批准手动 `DROP TABLE iot_sink.telemetry_sample, iot_sink.telemetry_inbox CASCADE; DROP SCHEMA IF EXISTS iot_sink;`，
并清理 `schema_migration_history` 中 V008 记录。禁止在故障处理中直接 DROP 业务数据或删除 history。

## 7. 审批

| 角色 | 意见 | 签名/日期 |
|---|---|---|
| owner | 本地集成库批准执行 V008 DDL（生产/共享库另批） | 2026-08-13 |
| DBA/架构复核 | V008 接入 runner + TD-003 §10/§13 对齐 | 2026-08-13 |

批准单号（APPROVAL）：`local-v008-verify-20260813`（本地集成库占位；生产审批单号待 owner 签发）

## 8. 执行结果

- 执行时间：2026-08-13 17:28（Asia/Shanghai）；runner `apply --step V008 --skip-precheck --yes` 返回 `STEP_DONE V008` / `apply SUCCEEDED`。
- 备份：`/tmp/td005-v008-backup/iot-device20_<时间戳>.sql`（本地验证用；生产窗口须用仓库外持久备份目录）。
- V008 history：`SUCCEEDED`；SHA-256 为 `693c0473386048567886b382c8c984ab98a267b7e7ce8659307b0d7395048469`；approval `local-v008-verify-20260813`。
- `iot_sink.telemetry_inbox` 与 `iot_sink.telemetry_sample` 两表存在（`\dt iot_sink.*` 确认）。
- 本次为本地集成库验证；生产/共享库 apply 须由 owner 签发独立审批单 + 仓库外备份 + precheck。
