# TD-005 模板 API 启用窗口申请单（2026-08-11）

> 双基线：平台功能计划 1.4.0 / EasyAIoT 项目开发宪法 1.5.0
> 依据：TD-005 1.0.36
> 目标：`full` 档位，仅重建 `iot-device`
> 当前状态：已按 owner 精确批准执行并验收通过

## 1. 唯一允许变更

- `EASYAIOT_POWER_MODEL_TEMPLATE_API_ENABLED=true`；
- `EASYAIOT_POWER_MODEL_BINDING_APPLY_API_ENABLED=false`；
- 保留已注入的 Config Tree HMAC Secret，容器明文 Secret 必须为 0；
- 仅用基础 Compose、Secret 覆盖层和 template API 覆盖层重建 `iot-device`。

明确禁止调用任何模板 API、写入 Canary 数据、开启 binding API、改变 role 111 权限、修改 Secret 文件、
修改 Topic 参数、执行 DDL、重建其他容器或调用 NODE。

## 2. 前置门禁

1. 阶段 2 只读预检 16/16 PASS；
2. tenant 122 / role 111 精确拥有 3900～3902，3903～3906 为 0；
3. tenant 122 的 14 类业务事实为 0；
4. Secret 文件门禁、Compose 合并解析、静态合同与 `READY_ONLY` 演练 PASS；
5. 当前 template/binding API=false，Secret 挂载不少于 32 字节。

## 3. 执行与自动回退

执行器：`.scripts/docker/power_model_template_api_activation_window.ps1`。默认只读，只有 `-Execute` 与批准
编号同时存在才进入变更。重建后必须通过 template-api 阶段预检、运行环境不变量和双库 Canary 只读验收。
任一失败时仅重建 `iot-device`，恢复 template/binding API=false，并保留 Secret 覆盖层；回退后重新验证
阶段 2、Secret 挂载和 Canary 零数据。

## 4. 批准边界

批准编号：`USER-APPROVAL-20260811-TD005-TEMPLATE-API-ACTIVATION`

本批准仅覆盖 template API 启用和单服务重建，不构成单次 Canary 写入批准。Canary identity、draft、
validate、publish 的任何调用仍需另行批准。

建议 owner 精确批准语句：

> 批准按 `USER-APPROVAL-20260811-TD005-TEMPLATE-API-ACTIVATION` 执行：保留已挂载的仓库外 Secret，
> 仅重建 `iot-device` 并启用 template API；binding API 保持关闭，不修改角色、Topic 或数据库，不调用
> 任何 API、不写 Canary 数据；失败时恢复 template API=false 并保留 Secret。

## 5. 审批与执行结果

- APPROVAL：`USER-APPROVAL-20260811-TD005-TEMPLATE-API-ACTIVATION`；
- owner 于 2026-08-11 精确批准 §4 建议语句的全部边界；
- PowerShell 语法、Compose 合并、Java 17 reactor 构建及执行器合同 6/6 PASS；`READY_ONLY` PASS 且
  容器 ID/启动时间未变化；
- 执行器仅重建 `iot-device`，最终 healthy；template API=true、binding API=false、release/events=true；
- Config Tree Secret 挂载 64 字节，容器明文 Secret=0；迁移 7/7、积压 0/0/0/0、业务基线 4/4/17、
  Topic 6/1/2,592,000,000 ms、消费组 6 分区在线 lag=0；
- role 111 仅有 3900～3902，3903～3906 为 0；tenant 122 的 14 类业务事实为 0；
- 未调用任何 API，未修改角色、Topic、数据库或 Secret 文件，未写 Canary 数据；自动回退未触发。
