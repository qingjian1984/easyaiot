# TD-005 Canary 角色授权窗口申请单

> 申请日期：2026-08-11
> 双基线：平台功能计划 1.4.0 / EasyAIoT 项目开发宪法 1.5.0
> 依据：TD-005 1.0.35
> 系统库目标：`postgres-server / ruoyi-vue-pro20`
> 业务只读核验库：`postgres-server / iot-device20`
> 当前状态：**已按独立批准执行并验收通过**

## 1. 推荐 Canary 边界

- 租户：`122 / 测试租户`，启用且有效期至 2029-04-27；
- 角色：`111 / 租户管理员 / tenant_admin`，租户 122 内当前关联 1 个活动用户；
- 业务影响面：tenant 122 在产品、设备、模板、版本、成员索引、绑定、审计、Outbox、Inbox、幂等、
  collector 发布/引用/审计/投影共 14 类事实中全部为 0；
- 当前 TD-005 角色关联：菜单 3900～3906 均为 0。

角色 111 已有 180 项菜单权限，因此它不是全局最小权限角色；本候选只保证“新增权限最小化”。选择它是为了
复用测试租户现有唯一操作用户，避免本窗口创建用户、密码或新角色。风险由 tenant 122 的空业务事实和租户
隔离补偿。若要求全局最小权限，必须改为另行创建专用用户/角色并重新申请，不得沿用本窗口。

## 2. 授权范围

仅向 `system_role_menu` 为角色 111 新增三项：

| 菜单 ID | 权限 | 用途 |
|---:|---|---|
| 3900 | `power:model-template:read` | 读取模板事实 |
| 3901 | `power:model-template:edit` | 创建 identity/draft、替换草稿 |
| 3902 | `power:model-template:publish` | validate/publish 冻结 action |

明确禁止授予 3903～3906：`import/upgrade/retire/power:system-template:manage`。本窗口不创建或修改用户、
角色、产品、模板，不启用 API，不注入 secret，不重启容器，不执行 canary 请求。

## 3. 冻结资产

| 资产 | SHA-256 |
|---|---|
| `preflight_canary_role.sql` | `3a3ccb1f2eb8415c565ec70b358bac52160d5303e1c3714edaacdb05d17620fb` |
| `preflight_canary_tenant_data.sql` | `3713a69dc2979f0b978ce6cac7155bfdb2f33215be850051ea0f66f1cf273f04` |
| `apply_canary_role_grant.sql` | `46d156ff111478fa8feb498fc97bc353c984c3377b08857dc3c1824870efd392` |
| `verify_canary_role_grant.sql` | `7dbc27ccc6ab38fe976d2129eef31e37cc2f2eb134f40c0e77cd1dbd1e33c520` |
| `rollback_canary_role_grant.sql` | `2923596827b73eb9cf5a9c1fbc3c0ae3b25d49bb7e6d3c837ef48a0d605b3b1d` |

任一 hash 变化后本申请自动失效。

## 4. 已有证据

- 2026-08-11 两个冻结 preflight 均在目标库实跑 PASS 并显式 ROLLBACK：role links=0、active user=1、
  tenant 122 residual rows=0；容器临时文件已清理。
- Java 17 静态安全合同 `PowerModelCanaryRoleGrantContractTest` 1/1 PASS。
- TD-005 权限 seed 已完成并验证七个权限事实存在；当前任何角色均未获得这些权限。

## 5. 执行步骤与验收

1. 复算 §3 五项 hash，并再次运行两个冻结只读 preflight；
2. 在仓库外重新备份 `ruoyi-vue-pro20`，校验 SHA-256 与 `pg_restore -l`；
3. 以 `psql -1 -v ON_ERROR_STOP=1` 执行 apply，5 秒锁忙即失败关闭；
4. 冻结 verify 必须只返回角色 111 的 3900～3902 三行，3903～3906 为 0；
5. 复核 tenant 122 业务事实仍为 0，API=false、secret 空、容器未重启；
6. 归档批准单、备份与验收证据。

## 6. 回滚

授权撤销优先使用精确 rollback，只删除 `creator=td005-canary-grant`、tenant 122、role 111、menu
3900～3902 的三行。发现部分授权、额外 TD-005 权限或 creator/tenant 漂移时拒绝删除。数据库恢复仅作为
精确回滚无法安全执行时的最后手段。

## 7. 审批

建议批准语句：

> 批准在 `postgres-server / ruoyi-vue-pro20` 自动备份成功后，仅向测试租户 122 的角色 111 授予
> TD-005 `read/edit/publish` 三项权限；不得授予 import/upgrade/retire/system-manage，不得启用 API、
> 注入 secret、重启容器、创建产品或执行 canary 请求。

| 角色 | 意见 | 签名/日期 |
|---|---|---|
| owner | 批准自动备份成功后仅向 tenant 122 / role 111 授予 3900～3902；禁止 3903～3906、API/Secret/容器/Canary 变更 | 2026-08-11 |
| DBA/架构复核 | 双库 preflight PASS；静态合同 1/1 PASS | 2026-08-11 |

批准单号（APPROVAL）：`USER-APPROVAL-20260811-TD005-CANARY-ROLE-GRANT`

## 8. 执行结果（2026-08-11）

- 五项冻结 SQL 的 SHA-256 均与 §3 一致；双库只读 preflight 再次 PASS 并显式 ROLLBACK：授权前
  role links=0、active users=1、tenant 122 residual rows=0；
- 在仓库外生成 `ruoyi-vue-pro20` custom-format 完整备份，大小 1,112,181 字节，SHA-256
  `8bfc32f04b2075f00a0dc49e3e68f7cd2428c266bb42b875116e09b009b8062b`；容器与宿主机 hash 一致，
  `pg_restore -l` 成功读取 1024 个 TOC 条目，容器临时文件已清理；
- apply 以单事务执行并返回 `INSERT 0 3`；冻结 verify 仅返回 tenant 122 / role 111 的菜单
  3900、3901、3902，creator 均为 `td005-canary-grant`，3903～3906 为 0；
- tenant 122 的 14 类业务事实继续为 0；template API=false、binding API=false，`iot-device` 启动时间
  保持 `2026-08-11T03:32:51.046352699Z`，未发生容器重启；阶段 2 运行预检 16/16 PASS；
- 未修改 Secret，未启用 API，未创建产品、模板、设备或绑定，未调用 Canary API。仓库外备份至少保留至
  Stage 3 Canary 验收完成后 7 天，删除需运维确认。
