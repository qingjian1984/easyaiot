# P02-M2-02C0：告警来源清单与迁移防漂移门禁本地验收记录

> 版本：1.0.0
> 日期：2026-08-25
> 状态：Verified-Local / Inventory Guard Only
> 双基线：[平台功能计划 1.5.0](../../架构设计/平台功能计划.md)、[EasyAIoT 项目开发宪法 1.6.0](../../开发规范/EasyAIoT项目开发宪法.md)
> 任务单：[P02-M2-02C0 1.0.2](./P02-M2-02C0-告警来源清单与迁移防漂移门禁任务单.md)

## 1. 验收结论

P02-M2-02C0 已完成本地静态实现：THRESHOLD、DEVICE_EVENT、VIDEO、AI、RUNTIME 五类来源及 7 个旧入口/旁路形成机器可读清单；无依赖 Node.js 门禁可从仓库根或 `WEB/` 执行，实时验证清单结构、仓库路径、代码 anchor、未授权阶段、外部 OPEN 项、敏感信息和 AI 新 publisher 漂移。

本记录只关闭 C0 静态清单门禁，不解锁 C1。未修改业务运行代码，未读取 `.env`，未访问网络、数据库、broker 或 Docker，未执行 V011/U011，也未创建 listener、producer、scheduler、Controller、SQL 或 capability 装配。

## 2. 实现范围

| 文件 | 作用 |
|---|---|
| `assets/td006-migration/alarm-source-inventory.json` | 固定双基线、五类来源、identity 缺口、证据路径/anchor、7 个 legacy entry、C0-C4 状态和 6 类外部 OPEN 项 |
| `WEB/scripts/verify-alarm-source-migration-boundaries.mjs` | Node 标准库静态校验、AI producer fail-closed 扫描和 16 类内置变异反例 |
| `WEB/package.json` | 增加 `verify:alarm-source-migration-boundaries` 命令；无依赖变化 |

脚本从 `import.meta.url` 推导仓库根，与调用目录无关；文件头明确其属于 PRD-02 迁移门禁而非 WEB 运行时/构建产物。`pnpm-lock.yaml` 未修改。

## 3. 验证证据

### 3.1 仓库根语法与正例

```powershell
node --check WEB/scripts/verify-alarm-source-migration-boundaries.mjs
node WEB/scripts/verify-alarm-source-migration-boundaries.mjs
```

结果：语法检查退出码 0；正例 `PASS checks=461 sources=5 legacy=7 scope=repository-static`。

### 3.2 变异反例

```powershell
node WEB/scripts/verify-alarm-source-migration-boundaries.mjs --self-test
```

结果：先完成 461 项实时正例，再输出 `SELF_TEST PASS cases=16`。反例覆盖来源缺失/重复、production scope、绝对/不存在/越界路径、anchor 缺失/计数漂移、legacy entry 缺失、AI producer 伪报、来源提前 READY、C1 提前批准、生产事实伪关闭、外部门禁遗漏、身份缺口标为可接受、敏感键及临时 AI 直接 publisher。

### 3.3 WEB 入口

```powershell
cd WEB
pnpm verify:alarm-source-migration-boundaries --self-test
```

结果：`PASS checks=461`，`SELF_TEST PASS cases=16`。证明脚本在 WEB 当前目录下仍从自身位置解析仓库根。

### 3.4 静态边界

- `git diff --check`：通过，只有换行转换提示；
- 脚本 import 仅为 `node:fs`、`node:os`、`node:path`、`node:url`；
- 未出现网络、子进程、数据库、Docker、`.env` 或第三方包调用；
- 运行时改动仅为 `WEB/package.json` 增加一个显式人工/CI 校验命令，不进入 WEB 构建或启动链。

## 4. C1 门禁现状表

| C1 解锁条件 | 状态 | 当前证据/缺口 |
|---|---|---|
| C0 机器清单与防漂移验收 | `CLOSED-LOCAL` | 本记录：461 项正例、16 类变异反例、双入口通过 |
| M1 telemetry identity/route 稳定 | `OPEN` | 尚无最终冻结且已提交的 M1 完成证据；不得从当前工作区进行推断 |
| tenant/site/device 权威解析 | `OPEN` | 五类来源仍存在 `0/default/缺失/未核验`，清单 fail-closed 记录 |
| THRESHOLD 稳定 cycle 规则 | `OPEN` | 旧链仍以“最新 OPEN”恢复；无显式周期 ID 合同 |
| DEVICE_EVENT 告警映射 | `OPEN` | 事件到严重度、RAISED/RECOVERED 和周期的映射未冻结 |
| 旧写与新入口原子或补偿决策 | `OPEN` | 同库/跨库失败语义、重放、对账和通知单 owner 未形成 Accepted ADR/任务冻结 |

结论：除 C0 外全部为 OPEN，C1 继续 `CLOSED`。本表只记录仓库可验证事实，不替代 M1、产品、架构或生产责任人签字。

## 5. 评审建议处置

- N-04：`CLOSED`。本记录已提供 C1 门禁逐项状态表，并明确 C0 不自动解锁 C1。
- N-05：`CLOSED-AS-PARTIAL-ADOPTION`。保留 `WEB/scripts/`，已增加归属说明、自定位路径和仓库根/WEB 双入口验证。
- N-01、N-03：`OPEN`，C1 解锁前处理 Inbox 三态裁决、transport 重试预算、终态重驱和 TD-006 0.1.3。
- N-02：`OPEN`，02A 临时 PostgreSQL 真实并发合同授权前冻结独立动作序号分配方案并整改；不得直接采用无行锁的 `MAX+1`。

## 6. 保持关闭的范围

- P02-M2-02A 临时 PostgreSQL 合同和任何真实 DDL；
- THRESHOLD/DEVICE_EVENT/VIDEO/AI/RUNTIME adapter；
- Kafka/MQTT Topic、group、acks、重试、DLQ 和 transport Bean；
- backfill、对账、统一查询/动作 API、WEB/APP 切读；
- standard/full capability 和生产容量声明。

本切片最高交付状态为 `Verified-Local / Inventory Guard Only`。
