# P02-M2-01 本地验收记录

> 版本：1.1.0  
> 日期：2026-08-24  
> 结论：Implemented / Verified-Local  
> 任务单：[P02-M2-01 告警领域合同与状态机](./P02-M2-01-告警领域合同与状态机任务单.md)  
> 双基线：[平台功能计划 1.5.0](../../架构设计/平台功能计划.md)、[EasyAIoT 项目开发宪法 1.6.0](../../开发规范/EasyAIoT项目开发宪法.md)

## 1. 验收结论

P02-M2-01 的无副作用本地合同切片已实现并通过 Sol 主审。交付包含告警状态/等级/事件枚举、不可变事件 Envelope、纯状态转换器、稳定错误码和两组 JUnit 合同测试。

本结论只证明本地编译合同和状态矩阵可用，不代表告警功能已经接入数据库、Kafka、HTTP、旧告警来源或部署 capability，也不批准生产启用。

## 2. 主审修正

- 恢复、解除忽略和忽略后确认成功时清空结果中的当前忽略态字段，历史由后续 action log 保存。
- 相同提议人/复核人即使上游误传独立复核标志，也稳定返回 `ALARM_REVIEWER_CONFLICT`。
- 补齐 `IGNORE_EXPIRED`、IGNORED 来源恢复、误报拒绝和同人复核不可覆盖测试。
- SDD 评审处置后，在既有事件枚举中加入 `device.alarm.suppression-decided.v1` 并补充 known-event 断言；未改状态机、Envelope 结构、外部依赖或生产配置。

## 3. 可复现验证

定向测试：

```powershell
mvn -f DEVICE/pom.xml -pl iot-device/iot-device-biz -am `
  '-Dmaven.test.skip=false' `
  '-Dtest=AlarmStateMachineTest,AlarmEventEnvelopeTest' `
  '-Dsurefire.failIfNoSpecifiedTests=false' test
```

结果：`BUILD SUCCESS`；`AlarmEventEnvelopeTest` 14 个、`AlarmStateMachineTest` 19 个，共 33 个测试，0 失败、0 错误、0 跳过。

本轮增量复核：`AlarmEventEnvelopeTest` 14 个测试再次通过，受影响 Reactor 33 个项目主代码编译通过。尝试同时重跑原 33 个测试时，Maven 在 `testCompile` 阶段因当前工作树类路径缺失多个告警/PowerModel 主类而被阻断，未进入 Surefire；因此不把该次运行记为“33 个已重跑”。状态机代码本轮未修改，原 19 个通过结果保留；该环境异常不授权跳过后续正式门禁。

编译：

```powershell
mvn -f DEVICE/pom.xml -pl iot-device/iot-device-biz -am compile
```

结果：`BUILD SUCCESS`；Reactor 33 个项目全部成功。

静态边界检查结果：冻结目录共 11 个文件；未发现 Spring、JDBC/Mapper、Kafka、TenantContext、系统时钟或随机数依赖。Maven 输出中的重复依赖、旧 API 和 Lombok Builder 警告来自既有仓库文件，本切片未修改对应文件。

## 4. 变更边界

实现仅位于：

```text
DEVICE/iot-device/iot-device-api/src/main/java/com/basiclab/iot/device/alarm/contract/
DEVICE/iot-device/iot-device-biz/src/main/java/com/basiclab/iot/device/alarm/domain/
DEVICE/iot-device/iot-device-biz/src/test/java/com/basiclab/iot/device/alarm/
```

未修改旧 `DeviceThresholdAlarm*`、Controller、SQL、Topic、application 配置、capability manifest、`iot-message`、`iot-sink`、WEB 或 VIDEO。本地工作区中其他 M1 变更不属于本任务，验收时未触碰。

## 5. 保持 OPEN

- V011/U011 仍是候选 SQL，未执行且未批准落库。
- 告警来源 Inbox、业务事务、Outbox relay、Kafka transport、API/权限和旧链迁移尚未实现。
- `power.alarm.core`、`power.alarm.advanced` 的档位与配额值尚未冻结，mini 无残留门禁尚未形成运行证据。
- 生产数据量、Topic/调用量画像和双写对账窗口仍需责任人输入。

因此下一切片 P02-M2-02 只能先做“DDL/Inbox/Outbox 的评审冻结与任务拆分”，不能直接执行 DDL、接入生产来源或启用 capability。
