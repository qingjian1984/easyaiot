# TD-005 电力 API 网关路由窗口申请单（2026-08-11）

> 双基线：平台功能计划 1.4.0 / EasyAIoT 项目开发宪法 1.5.0
> 依据：TD-005 1.0.38
> 目标：仅构建并首次创建 `iot-gateway`
> 状态：COMPLETED / FIRST DEPLOY PASS

## 1. 唯一允许变更

将仓库已验证的 `device-power-model-api` 路由部署到运行网关：`/api/v1/power/**` 原样转发到
`lb://device-server`，不使用 StripPrefix 或 RewritePath。仅允许构建 `iot-gateway:latest` 并重建
`iot-gateway`；执行前必须保留当前镜像 ID/回退标签。

明确禁止重建 `iot-device` 或其他容器、修改 template/binding API、Secret、角色、数据库、Topic、Nacos
配置，不得登录用户、生成/读取 token、调用任何业务 API或写 Canary 数据。

## 2. 前置与验收

- 路由/Canary 资产合同 3/3 PASS；`iot-gateway` Java 17 package BUILD SUCCESS；
- 执行前记录当前网关容器与镜像 ID，当前网关必须 healthy；
- 新镜像仅来自当前提交的 gateway jar，重建命令必须限定 `--no-deps iot-gateway`；
- 重建后网关 healthy，路由配置存在且 `/api/v1/power/**` 不剥离/改写；
- template-api 阶段 16/16、角色、tenant 空数据和数据库积压保持原值；
- 失败时用保留的原镜像标签仅回退 `iot-gateway` 并复验健康。

## 3. 批准边界

批准编号候选：`USER-APPROVAL-20260811-TD005-POWER-API-GATEWAY-ROUTE`

本窗口完成不构成 Canary 写入批准。路由部署后仍必须由 `user 113 / aoteman` 正常登录取得未过期短时令牌，
再对 identity→draft→validate→publish 单次写入另行批准。

建议批准语句：

> 批准按 `USER-APPROVAL-20260811-TD005-POWER-API-GATEWAY-ROUTE` 执行：保留当前网关镜像回退标签，
> 仅构建并重建 `iot-gateway` 以部署 `/api/v1/power/**` 原样转发路由；不得重建其他容器、修改 API 开关、
> Secret、角色、数据库或 Topic，不得获取 token、调用业务 API 或写 Canary；失败时仅回退网关。

## 4. 首次部署修正与执行结果

首次执行预检确认当前不存在 `iot-gateway` 容器和 `iot-gateway:latest` 镜像，无法满足原窗口“当前网关
healthy + 保留当前镜像回退标签”的前提，故在构建前停止且未产生运行变更。owner 随后批准：

> `USER-APPROVAL-20260811-TD005-POWER-API-GATEWAY-FIRST-DEPLOY`：确认当前无 `iot-gateway` 容器和
> `iot-gateway:latest` 镜像，仅构建并首次创建 `iot-gateway`；失败时仅删除新建网关并恢复“无网关”状态；
> 其余原审批限制保持不变。

执行结果：

- 目标路由块精确包含 `Path=/api/v1/power/**` → `lb://device-server`，目标块无 StripPrefix/RewritePath；
- 仅构建 `iot-gateway:latest`，仅以 `docker compose ... up -d --no-deps iot-gateway` 首次创建网关；
- 网关容器 `dc358ab7d385` healthy，镜像 ID `sha256:e68504341ffcb6f863bcea243069ae05cc72322c90bb99823e935b6d2a3268f7`；
- 容器与宿主 gateway JAR SHA-256 均为 `2A91097BD2AC616E5CD82A6A15D55901C07B7EBB5E0CDB31CD153B615C96992E`；
- `iot-device`、`iot-system`、`iot-infra`、PostgreSQL、Kafka 均未重建，template-api 阶段 17 项 PASS；
- role 111 仍精确拥有 3900～3902，禁止权限为 0；tenant 122 的 14 类事实仍为 0；
- 未获取 token、未调用业务 API、未写 Canary；首次部署成功，删除式回退未触发。

详细证据见
[`assets/td005-canary/gateway-first-deploy-window-execution-20260811.md`](./assets/td005-canary/gateway-first-deploy-window-execution-20260811.md)。
