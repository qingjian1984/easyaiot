# TD-005 Canary 浏览器认证窗口申请单（2026-08-11）

> 双基线：平台功能计划 1.4.0 / EasyAIoT 项目开发宪法 1.5.0
> 依据：TD-005 1.0.40
> 状态：BLOCKED_BROWSER_CONTROL / APPROVED BUT NOT EXECUTED
> 登录面：`http://localhost:8888`

## 1. 唯一允许动作

由用户本人在受控浏览器登录页输入 tenant=`测试租户`、username=`aoteman`、现有密码及必要验证码；必须
设置 `rememberMe=false`。只允许登录页静态资源、租户查询、验证码 get/check、`/system/auth/login` 和
`/system/auth/get-permission-info`；浏览器层必须阻断 `/api/v1/power/**` 及其他非认证 API。

成功后 access token 仅保留在该浏览器会话存储中，不导出、不显示、不复制到终端、仓库、文档或聊天。
验收只通过只读数据库元数据确认 user 113 新增一个未过期 access token、有效期不超过 1800 秒，并复核
role 111 权限与 tenant 122 空数据事实。

## 2. 明确禁止

- 不由自动化输入、读取、记录或恢复密码；密码和验证码必须由用户本人输入；
- 不选择“记住我”，不刷新 token，不读取 token 内容或摘要；
- 不调用电力、产品、设备、绑定或其他业务 API，不写 Canary；
- 不修改 API 开关、Secret、角色、数据库、Topic、Nacos、capability 或任何容器；
- 登录失败一次立即停止，不自动重试，不重置密码，不查询密码摘要；
- 本窗口成功不构成 Canary 写入批准。

## 3. 验收与失效

- 登录响应成功且 permission-info 返回 user 113 / tenant 122 / role 111；
- 数据库只读元数据确认恰有一个未过期 access token，剩余有效期在 1～1800 秒；
- role 111 仍仅有 3900～3902，tenant 122 residual_rows=0；
- 浏览器网络记录中没有 `/api/v1/power/**` 或其他非认证 API；
- 若后续 Canary 窗口未在 token 有效期内获批，令牌自然过期，不得在本窗口刷新。

建议批准语句：

> 批准按 `USER-APPROVAL-20260811-TD005-CANARY-BROWSER-AUTH` 执行：仅打开
> `http://localhost:8888`，由我本人输入测试租户、aoteman 现有密码和验证码，`rememberMe=false`；仅允许
> 租户查询、验证码、登录和 permission-info，请阻断其他 API。token 仅留浏览器会话，不导出、不显示、
> 不刷新；登录失败一次即停止，不重置密码；不得修改容器、API 开关、Secret、角色、数据库、Topic、Nacos
> 或 capability，不得调用电力业务 API或写 Canary。

## 4. 首次执行尝试

- 批准：`USER-APPROVAL-20260811-TD005-CANARY-BROWSER-AUTH`；
- 应用内浏览器控制在导航前无法建立连接，原因是主机拒绝访问浏览器连接所需的用户配置元数据；
- 页面未打开，租户查询、验证码、login、permission-info 请求均未发出；
- 未改用其他浏览器控制方式，未直接调用登录 API；
- 事后只读复核 token=0、允许权限=3、禁止权限=0，WEB/gateway/system/device 均未变化且 healthy；
- 本次不是登录失败，不消耗“一次失败即停止”的登录尝试；窗口保持未执行。

详细证据见
[`assets/td005-canary/browser-auth-window-attempt-20260811.md`](./assets/td005-canary/browser-auth-window-attempt-20260811.md)。
