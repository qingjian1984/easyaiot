# TD-001 V007 发布单派生身份专项评审报告

> 日期：2026-08-10
> 双基线：平台功能计划 1.4.0 / EasyAIoT 项目开发宪法 1.5.0
> 结论：**通过 runner 增链评审；不构成任何目标实例执行授权**

## 1. 评审范围

- `V007__collector_release_derivation_identity.sql`
- `U006__collector_release_derivation_identity.sql`
- `CollectorConfigReleasePort` 与 `PowerModelCollectorEventHandlers` 的候选定位/确认人合同
- TD-001 1.0.13、TD-005 1.0.17 的四项采集策略事实边界

## 2. 发现与处置

| 编号 | 级别 | 发现 | 处置 | 状态 |
|---|---|---|---|---|
| V7-H01 | HIGH | `productId/bindingRevision` 仅做正数检查会接受跨租户或不存在绑定 | 增加 `(tenant_id, product_id, binding_revision)` 到绑定历史的复合 FK | CLOSED |
| V7-H02 | HIGH | `sourceEventId` 无来源约束，消费者可能命中伪造候选 | Outbox 增加 `(tenant_id,event_id)` 唯一键，发布单增加同租户复合 FK | CLOSED |
| V7-M01 | MEDIUM | 回滚自由原因被拼入发布稳定码/协调审计，存在枚举漂移与敏感信息扩散 | 发布单原因只允许二值稳定码；自由原因仅留在原事件/领域审计受控字段 | CLOSED |
| V7-M02 | MEDIUM | 回滚 V1 Schema 不携带模板字段，旧处理器却强制读取 | 回滚按 `bindingRevision` 解析，`templateCode/templateVersion` 传 null | CLOSED |
| V7-M03 | MEDIUM | V1 Schema 的 bigint ID 为字符串，旧处理器只接受 JSON number | 改为正 bigint 十进制字符串解析并补非法确认人反例 | CLOSED |
| V7-M04 | MEDIUM | V003 不可变触发器遗漏 `node_id/base_version/rollback_from_version` | V007 替换函数时纳入三字段，发布目标与回滚来源不得事后改写 | CLOSED |
| V7-L01 | LOW | 迁移对非空发布单无通用回填策略 | 失败关闭并给出稳定前置错误；当前目标只读计数 release/projection/binding/outbox 均为 0，未来非空环境必须独立回填设计 | ACCEPTED GATE |

## 3. 验证结论

- Java 17 clean 合同：13/13 PASS。
- 两个一次性 PostgreSQL 临时库完成基础链及 V001→V002→V003→V004→V007 完整依赖链验证。
- 首次执行、状态推进、事件唯一、快照/派生身份不可变、绑定/Outbox 跨租户与不存在引用拒绝、原因码约束、中文 COMMENT、invalid index、V007 非空重放拒绝、U006 非空拒绝/空表卸载均 PASS。
- 临时库全部删除；`iot-device20` 只读确认四张相关表均为 0，未执行 V007。

## 4. 后续强制门禁

1. runner 可增链 V007 并做 dry-run；apply 仍必须独立审批、仓库外备份且仅执行明确步骤。
2. `CollectorConfigReleasePort` 实现必须在单事务内锁定唯一 VALIDATED 候选，使用状态/rowVersion CAS 推进 PUBLISHED，并按更高 projectionRevision 更新 workload 投影；0 行或多行均终态失败。
3. 第四端口实现和真实 PG 合同完成前不得装配；不得因 V007 就绪提前启用消费者业务处理器。
4. 全量安装 dump 与角色授权仍需同步，未完成前 TD-001 保持 In Review。
