spring:
  main:
    allow-circular-references: true # 允许循环依赖，因为项目是三层架构，无法避免这个情况。
    allow-bean-definition-overriding: true # 允许 Bean 覆盖，例如说 Feign 等会存在重复定义的服务

  # Servlet 配置
  servlet:
    # 文件上传相关配置项
    multipart:
      max-file-size: 16MB # 单个文件大小
      max-request-size: 32MB # 设置总上传的文件大小
  mvc:
    pathmatch:
      matching-strategy: ANT_PATH_MATCHER # 解决 SpringFox 与 SpringBoot 2.6.x 不兼容的问题，参见 SpringFoxHandlerProviderBeanPostProcessor 类

  # Jackson 配置项
  jackson:
    serialization:
      write-dates-as-timestamps: true # 设置 LocalDateTime 的格式，使用时间戳
      write-date-timestamps-as-nanoseconds: false # 设置不使用 nanoseconds 的格式。例如说 1611460870.401，而是直接 1611460870401
      write-durations-as-timestamps: true # 设置 Duration 的格式，使用时间戳
      fail-on-empty-beans: false # 允许序列化无属性的 Bean

--- #################### 接口文档配置 ####################

springdoc:
  api-docs:
    enabled: true # 1. 是否开启 Swagger 接文档的元数据
    path: /v3/api-docs
  swagger-ui:
    enabled: true # 2.1 是否开启 Swagger 文档的官方 UI 界面
    path: /swagger-ui.html
  default-flat-param-object: true # 参见 https://doc.xiaominfo.com/docs/faq/v4/knife4j-parameterobject-flat-param 文档

knife4j:
  enable: true # 2.2 是否开启 Swagger 文档的 Knife4j UI 界面
  setting:
    language: zh_cn

--- #################### BasicLab 相关配置 ####################

iot:
  info:
    version: 1.0.0
    base-package: com.basiclab.iot.video
  web:
    admin-ui:
      url: http://dashboard.iot.iocoder.cn # Admin 管理后台 UI 的地址
  swagger:
    title: 视频 SDK 接入
    description: 海康 HCNetSDK / 大华 NetSDK 设备注册信息查询等接口
    version: ${iot.info.version}
    base-package: ${iot.info.base-package}

--- #################### 海康 / 大华 SDK 本地库路径（可选） ####################

# 海康 HCNetSDK 动态库路径（不填则默认 user.dir/iot-video-sdk/...）
hik:
  hcnetsdk:
    windows-dll: ""
    linux-so: ""

debug: false
